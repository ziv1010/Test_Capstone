{
  "method_proposal": [
    {
      "method_id": "M1",
      "name": "Naive Baseline",
      "category": "baseline",
      "description": "Predicts the last observed value as the next value",
      "implementation_code": "def predict_naive(train_df, test_df, target_col, date_col, **params):\n    import pandas as pd\n    import numpy as np\n\n    # Naive prediction: last value\n    last_value = train_df[target_col].iloc[-1]\n    predictions = [last_value] * len(test_df)\n\n    return pd.DataFrame({'predicted': predictions}, index=test_df.index)",
      "required_libraries": [
        "pandas",
        "numpy"
      ],
      "hyperparameters": {},
      "expected_strengths": [
        "Extremely simple",
        "No parameters to tune"
      ],
      "expected_weaknesses": [
        "Cannot capture trends or seasonality"
      ]
    },
    {
      "method_id": "M2",
      "name": "Exponential Smoothing",
      "category": "statistical",
      "description": "Simple exponential smoothing for baseline trend",
      "implementation_code": "def predict_exponential_smoothing(train_df, test_df, target_col, date_col, alpha=0.2):\n    import pandas as pd\n    import numpy as np\n    from statsmodels.tsa.holtwinters import SimpleExpSmoothing\n\n    # Fit model\n    model = SimpleExpSmoothing(train_df[target_col])\n    fitted = model.fit(smoothing_level=alpha, optimized=False)\n\n    # Forecast\n    forecast = fitted.forecast(len(test_df))\n\n    return pd.DataFrame({'predicted': forecast.values}, index=test_df.index)",
      "required_libraries": [
        "pandas",
        "numpy",
        "statsmodels"
      ],
      "hyperparameters": {
        "alpha": 0.2
      },
      "expected_strengths": [
        "Handles simple trends",
        "Few parameters"
      ],
      "expected_weaknesses": [
        "Cannot handle seasonality"
      ]
    },
    {
      "method_id": "M3",
      "name": "Random Forest with Lags",
      "category": "ml",
      "description": "Random forest using lag features for forecasting",
      "implementation_code": "def predict_random_forest(train_df, test_df, target_col, date_col, n_lags=7):\n    import pandas as pd\n    import numpy as np\n    from sklearn.ensemble import RandomForestRegressor\n\n    def create_lag_features(df, target_col, n_lags):\n        result = df.copy()\n        for i in range(1, n_lags + 1):\n            result[f'lag_{i}'] = result[target_col].shift(i)\n        return result.dropna()\n\n    # Create features\n    train_with_lags = create_lag_features(train_df, target_col, n_lags)\n    feature_cols = [f'lag_{i}' for i in range(1, n_lags + 1)]\n\n    X_train = train_with_lags[feature_cols]\n    y_train = train_with_lags[target_col]\n\n    # Train model\n    model = RandomForestRegressor(n_estimators=100, random_state=42)\n    model.fit(X_train, y_train)\n\n    # Predict\n    last_values = list(train_df[target_col].tail(n_lags).values)\n    predictions = []\n\n    for _ in range(len(test_df)):\n        X_pred = pd.DataFrame([last_values[::-1]], columns=feature_cols)\n        pred = model.predict(X_pred)[0]\n        predictions.append(pred)\n        last_values = [pred] + last_values[:-1]\n\n    return pd.DataFrame({'predicted': predictions}, index=test_df.index)",
      "required_libraries": [
        "pandas",
        "numpy",
        "scikit-learn"
      ],
      "hyperparameters": {
        "n_lags": 7,
        "n_estimators": 100
      },
      "expected_strengths": [
        "Handles non-linear patterns",
        "Robust to outliers"
      ],
      "expected_weaknesses": [
        "Requires feature engineering",
        "May overfit"
      ]
    }
  ],
  "data_split": {
    "strategy_type": "temporal",
    "date_column": "Season",
    "target_column": "Production-2024-25",
    "train_period": "70% of data",
    "validation_period": "15% of data",
    "test_period": "15% of data"
  }
}